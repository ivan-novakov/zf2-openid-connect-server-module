- session manager: put expiration times in config

- implement user attribute filtering/validating:
  - handle multivalue Shibboleth attributes
  - check for invalid values

- client validator
  - validate match between client ID and redirect URI
  - ...
  
- more verbose logs
  - show user ID in log  
  
- endpoint for logout (delete login session)

- re-organize views and renderers

- add dispatch controller tests